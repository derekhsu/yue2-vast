# Required: bearer key for the inference API (also enforced through Caddy)
MUSIC_API_KEY=change-me

# Optional: only if HF repos ever require auth (currently public)
HF_TOKEN=

# Optional overrides
# DEPLOY_REPO=https://github.com/derekhsu/yue2-vast.git
# DEPLOY_REF=main
# MEMORY_BUDGET_GIB=24
# HF_HOME=/workspace/hf
